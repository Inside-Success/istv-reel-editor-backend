services:
  - type: web
    name: istv-reel-editor-backend
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: REVAI_API_KEY
        sync: false
      - key: CLAUDE_API_KEY
        sync: false
